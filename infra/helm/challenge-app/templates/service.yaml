apiVersion: v1
kind: Service
metadata:
  name: {{ include "challenge-app.fullname" . }}
  labels:
    {{- include "challenge-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "challenge-app.selectorLabels" . | nindent 4 }}
