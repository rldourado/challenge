apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "challenge-app.fullname" . }}-test-connection"
  labels:
    {{- include "challenge-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "challenge-app.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
